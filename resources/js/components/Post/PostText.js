
export default function PostText(props) {
    return   <div className="post-container-text">
    When the release date matches current date of that time, the upvote/downvote functionalityy locks and is replaced with rating func
    tion where users can vote from 1-5 if they think the prediction was spot
     on or not. Users can also sort the posts whether they have already in stage 1(upvote/downvote) or stage 2 rating(1-5).
    </div>
  }
